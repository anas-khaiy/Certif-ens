port: 7880
bind_addresses:
  - 0.0.0.0

rtc:
  tcp_port: 7881
  udp_port: 7882
  # IMPORTANT: set this to the server's actual public IP
  # Run: curl -s ifconfig.me   on the server to get the IP
  node_ip: 196.200.178.203   # public IP of the VPS

keys:
  devkey: devsecret32charslongtofitrequirement

logging:
  level: info
